<div class="max-w-xl mx-auto px-6 py-16">
  <%= render 'shared/page_header', 
      title: "크루 만들기", 
      icon: "👟", 
      description: "나만의 러닝 크루를 만들고 동료들과 함께 달리세요.",
      centered: true %>

  <div class="bg-white rounded-[2.5rem] shadow-xl shadow-gray-200 border border-gray-100 p-10">
    <%= form_with(model: @crew, local: true, class: "space-y-6") do |f| %>
       <% if @crew.errors.any? %>
        <div class="p-4 bg-red-50 text-red-600 rounded-xl text-sm font-medium mb-6">
          <ul>
            <% @crew.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div>
        <%= f.label :name, "크루 이름", class: "block text-sm font-bold text-gray-700 mb-2" %>
        <%= f.text_field :name, class: "w-full px-5 py-4 rounded-2xl border-gray-200 focus:ring-black focus:border-black shadow-sm transition-all text-lg font-medium", placeholder: "멋진 크루 이름" %>
      </div>
      
      <div>
        <%= f.label :description, "크루 소개", class: "block text-sm font-bold text-gray-700 mb-2" %>
        <%= f.text_area :description, rows: 4, class: "w-full px-5 py-4 rounded-2xl border-gray-200 focus:ring-black focus:border-black shadow-sm transition-all font-medium", placeholder: "어떤 사람들이 모이는 크루인가요?" %>
      </div>
      
      <div class="pt-4">
        <%= f.submit "크루 생성하기", class: "w-full py-4 bg-black hover:bg-gray-800 text-white font-black rounded-2xl shadow-lg transition-all active:scale-[0.98] text-lg cursor-pointer" %>
      </div>
      
      <div class="text-center mt-4">
        <%= link_to "취소", crews_path, class: "text-sm text-gray-400 hover:text-gray-600 font-bold" %>
      </div>
    <% end %>
  </div>
</div>
