<div class="max-w-6xl mx-auto px-6 py-16">
  <div class="bg-gray-900 rounded-[3rem] p-12 text-white shadow-2xl shadow-gray-200 mb-12 relative overflow-hidden">
    <!-- Background Decorator -->
    <div class="absolute top-0 right-0 w-96 h-96 bg-blue-600 rounded-full blur-[120px] opacity-40 mix-blend-screen pointer-events-none"></div>
    <div class="absolute bottom-0 left-0 w-64 h-64 bg-indigo-600 rounded-full blur-[100px] opacity-30 mix-blend-screen pointer-events-none"></div>
    
    <div class="relative z-10 grid md:grid-cols-2 gap-12">
      <div>
         <span class="px-4 py-1.5 bg-white/10 backdrop-blur rounded-full text-xs font-black uppercase tracking-widest text-blue-200 border border-white/10 mb-6 inline-block">
           Crew Profile
         </span>
         <h1 class="text-5xl md:text-6xl font-black tracking-tighter mb-6 leading-none"><%= @crew.name %></h1>
         <p class="text-lg text-gray-300 font-medium leading-relaxed mb-8"><%= @crew.description %></p>
         
         <div class="flex items-center space-x-6">
            <div>
               <p class="text-[10px] text-gray-500 font-black uppercase tracking-widest mb-1">Leader</p>
               <div class="flex items-center space-x-3 bg-white/5 p-2 pr-4 rounded-full border border-white/5">
                 <div class="w-8 h-8 rounded-full bg-blue-500 flex items-center justify-center text-xs font-black ring-2 ring-gray-900">
                   <%= @crew.leader.name[0] %>
                 </div>
                 <p class="font-bold text-sm"><%= @crew.leader.name %></p>
               </div>
            </div>
            
            <div class="h-8 w-px bg-white/10"></div>
            
            <div class="flex space-x-6">
                <div>
                   <p class="text-[10px] text-gray-500 font-black uppercase tracking-widest mb-1">Members</p>
                   <p class="font-bold text-2xl text-white"><%= @members.count %></p>
                </div>
                <div>
                   <p class="text-[10px] text-gray-500 font-black uppercase tracking-widest mb-1">Activities</p>
                   <p class="font-bold text-2xl text-white"><%= @recent_activities.count %></p>
                </div>
            </div>
         </div>
      </div>
      
      <div class="flex flex-col justify-center items-center md:items-end">
        <div class="bg-white/5 backdrop-blur-xl rounded-[2rem] p-8 border border-white/10 text-center w-full max-w-sm group hover:bg-white/10 transition-colors cursor-pointer relative overflow-hidden">
           <div class="absolute inset-0 bg-gradient-to-br from-blue-500/20 to-purple-500/20 opacity-0 group-hover:opacity-100 transition-opacity"></div>
           <p class="text-[10px] text-gray-400 font-black uppercase tracking-widest mb-4">Invite Code</p>
           <p class="text-4xl font-black text-white tracking-widest font-mono select-all mb-2"><%= @crew.code %></p>
           <p class="text-xs text-blue-300 font-bold group-hover:text-white transition-colors flex items-center justify-center">
             <svg class="w-4 h-4 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z" /></svg>
             Click to copy
           </p>
        </div>
        <div class="mt-6 w-full max-w-sm space-y-3">
          <p class="text-xs text-gray-400 text-center md:text-right">
            대회 참가 신청 시 이 코드를 입력하면 크루원으로 등록됩니다.
          </p>
          <%= link_to races_path, class: "flex items-center justify-center w-full py-3.5 bg-blue-600 hover:bg-blue-500 text-white font-black rounded-xl transition-all text-sm" do %>
            <svg class="w-4 h-4 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" /></svg>
            대회 신청하러 가기
          <% end %>
        </div>
      </div>
    </div>
  </div>

  <div class="grid lg:grid-cols-3 gap-12">
    <!-- Recent Activities -->
    <div class="lg:col-span-2 space-y-8">
      <div class="flex items-center justify-between">
        <h2 class="text-2xl font-black text-gray-900 flex items-center">
           <span class="w-2 h-8 bg-blue-600 rounded-full mr-4"></span>
           최근 활동 (Activities)
        </h2>
      </div>
      
      <% if @recent_activities.any? %>
        <div class="bg-white rounded-[2.5rem] shadow-sm border border-gray-100 overflow-hidden divide-y divide-gray-50">
          <% @recent_activities.each do |reg| %>
            <div class="p-6 flex items-center justify-between group hover:bg-gray-50 transition-colors">
               <div class="flex items-center space-x-5">
                 <div class="w-14 h-14 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-2xl flex items-center justify-center shadow-lg shadow-blue-200">
                    <span class="text-white font-black text-xs">RUN</span>
                 </div>
                 <div>
                    <h4 class="font-black text-lg text-gray-900 mb-1 group-hover:text-blue-600 transition-colors"><%= reg.race.title %></h4>
                    <div class="flex items-center text-xs font-bold text-gray-400 space-x-2">
                       <span class="flex items-center text-gray-600">
                         <div class="w-5 h-5 rounded-full bg-gray-100 flex items-center justify-center mr-1 text-[10px] font-black">
                           <%= reg.user.name[0] %>
                         </div>
                         <%= reg.user.name %>
                       </span>
                       <span>•</span>
                       <span><%= time_ago_in_words(reg.created_at) %> ago</span>
                    </div>
                 </div>
               </div>
               <%= link_to race_path(reg.race), class: "hidden sm:flex items-center text-gray-300 hover:text-blue-600 transition-colors" do %>
                 <span class="text-xs font-black uppercase tracking-widest mr-2">View</span>
                 <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" /></svg>
               <% end %>
            </div>
          <% end %>
        </div>
      <% else %>
        <div class="bg-gray-50 rounded-[2.5rem] p-16 text-center border border-dashed border-gray-200">
           <div class="w-20 h-20 bg-white rounded-full mx-auto mb-6 flex items-center justify-center shadow-sm text-gray-300">
             <svg class="w-10 h-10" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" /></svg>
           </div>
           <h3 class="text-xl font-black text-gray-900 mb-2">아직 활동 기록이 없습니다</h3>
           <p class="text-gray-500 font-medium mb-8">대회에 참가하고 첫 번째 기록을 남겨보세요!</p>
           <%= link_to "참가할 대회 찾기", races_path, class: "inline-flex items-center px-8 py-4 bg-gray-900 hover:bg-black text-white font-black rounded-2xl shadow-xl transition-all active:scale-[0.98]" %>
        </div>
      <% end %>
    </div>

    <!-- Right Sidebar -->
    <div class="space-y-8">
      <!-- Members List -->
      <section>
        <h2 class="text-2xl font-black text-gray-900 mb-6 flex items-center">
           <span class="w-2 h-8 bg-indigo-600 rounded-full mr-4"></span>
           멤버 (<%= @members.count %>)
        </h2>
        
        <div class="bg-white p-6 rounded-[2.5rem] shadow-sm border border-gray-100">
           <div class="grid grid-cols-4 gap-4">
             <% @members.each do |member| %>
               <div class="flex flex-col items-center group cursor-pointer">
                  <div class="w-14 h-14 bg-gray-50 rounded-2xl flex items-center justify-center text-gray-500 font-black text-sm mb-2 group-hover:bg-indigo-50 group-hover:text-indigo-600 transition-all shadow-sm group-hover:shadow-md ring-2 ring-transparent group-hover:ring-indigo-100" title="<%= member.name %>">
                     <%= member.name[0] %>
                  </div>
                  <p class="text-[10px] text-gray-400 font-bold truncate w-full text-center group-hover:text-gray-600 transition-colors"><%= member.name %></p>
               </div>
             <% end %>
             
             <!-- Invite Button -->
             <button class="flex flex-col items-center group">
                <div class="w-14 h-14 border-2 border-dashed border-gray-200 rounded-2xl flex items-center justify-center text-gray-300 group-hover:border-blue-400 group-hover:text-blue-400 transition-all bg-gray-50/50">
                   <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/></svg>
                </div>
                <p class="text-[10px] text-gray-400 font-bold mt-2 group-hover:text-blue-500 transition-colors">Invite</p>
             </button>
           </div>
        </div>
      </section>
      
      <!-- Crew Ranking -->
      <section class="bg-gradient-to-br from-gray-900 to-gray-800 rounded-[2.5rem] p-8 text-white text-center relative overflow-hidden">
        <h3 class="font-black text-xl mb-2 relative z-10">크루 랭킹</h3>
        <p class="text-xs text-gray-400 mb-6 relative z-10">누적 거리 기준 TOP 멤버</p>

        <div class="space-y-3 relative z-10">
          <% medal = ["🥇", "🥈", "🥉"] %>
          <% @top_members.each_with_index do |member, i| %>
            <div class="flex items-center justify-between <%= i == 0 ? 'bg-white/10' : 'bg-white/5' %> p-3 rounded-xl border border-white/5">
              <div class="flex items-center space-x-3">
                <span class="text-lg"><%= medal[i] %></span>
                <div class="w-8 h-8 rounded-full bg-white/10 flex items-center justify-center text-xs font-black">
                  <%= member.name[0] %>
                </div>
                <span class="font-bold text-sm"><%= member.name %></span>
              </div>
              <span class="text-xs font-mono text-gray-300"><%= number_with_precision(member.total_distance, precision: 1) %> km</span>
            </div>
          <% end %>
          <% if @top_members.empty? %>
            <p class="text-sm text-gray-500 py-4">아직 기록이 없습니다</p>
          <% end %>
        </div>
      </section>
    </div>
  </div>
</div>
