<div class="max-w-6xl mx-auto px-6 py-16">
  <% content_for :header_actions do %>
    <div class="flex flex-col md:flex-row items-center gap-3 w-full md:w-auto">
      <% if user_signed_in? %>
        <%= link_to new_crew_path, class: "h-10 w-full md:w-auto px-6 bg-blue-600 hover:bg-blue-700 text-white rounded-xl shadow-lg shadow-blue-50 transition-all border-none flex items-center justify-center font-bold text-sm" do %>
          크루 만들기
        <% end %>
      <% else %>
        <%= link_to new_user_session_path, class: "h-10 w-full md:w-auto px-6 bg-blue-600 hover:bg-blue-700 text-white rounded-xl shadow-lg shadow-blue-50 transition-all border-none flex items-center justify-center font-bold text-sm" do %>
          로그인하고 크루 참여
        <% end %>
      <% end %>
    </div>
  <% end %>

  <%= render 'shared/page_header',
      title: "러닝 크루",
      icon: "🏃‍♂️",
      description: "함께 달릴 동료를 찾아보세요. 혼자보다 함께할 때 더 멀리 갈 수 있습니다." %>

  <%# 비로그인 CTA 배너 %>
  <% unless user_signed_in? %>
    <div class="mt-10 relative overflow-hidden rounded-2xl bg-gradient-to-r from-blue-600 to-indigo-600 p-8 text-white shadow-xl shadow-blue-200">
      <div class="absolute -right-10 -top-10 w-48 h-48 bg-white/10 rounded-full"></div>
      <div class="absolute -right-4 bottom-0 w-32 h-32 bg-white/5 rounded-full"></div>
      <div class="relative">
        <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-6">
          <div>
            <p class="text-blue-100 text-sm font-bold uppercase tracking-widest mb-2">함께 달리면 더 빠릅니다</p>
            <h2 class="text-2xl font-black mb-2">크루에 가입하거나 직접 만들어보세요</h2>
            <p class="text-blue-100 text-sm leading-relaxed">
              같은 목표를 가진 러너들과 함께 성장하세요.<br class="hidden md:block">
              대회 참가, 기록 공유, 단체 훈련까지 — 혼자는 어렵지만 함께라면 가능합니다.
            </p>
          </div>
          <div class="flex flex-col sm:flex-row gap-3 flex-shrink-0">
            <%= link_to new_user_registration_path, class: "inline-flex items-center justify-center px-6 py-3 bg-white text-blue-600 font-black rounded-xl hover:bg-blue-50 transition-all text-sm shadow-lg whitespace-nowrap" do %>
              무료로 시작하기
            <% end %>
            <%= link_to new_user_session_path, class: "inline-flex items-center justify-center px-6 py-3 bg-white/10 text-white font-bold rounded-xl hover:bg-white/20 transition-all text-sm border border-white/20 whitespace-nowrap" do %>
              로그인
            <% end %>
          </div>
        </div>
      </div>
    </div>
  <% end %>

  <% if @crews.any? %>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mt-12">
      <% @crews.each do |crew| %>
        <%= link_to crew_path(crew), class: "group block" do %>
          <div class="bg-white rounded-2xl overflow-hidden border border-gray-200 hover:shadow-xl transition-all h-full flex flex-col">
            <!-- Image -->
            <div class="aspect-[16/10] relative overflow-hidden bg-gradient-to-br from-blue-500 to-indigo-600">
              <div class="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent"></div>

              <div class="absolute top-4 right-4">
                <span class="bg-white text-gray-900 font-bold px-3 py-1.5 rounded-lg text-xs shadow-lg">
                  <%= crew.members.count %>명
                </span>
              </div>

              <div class="absolute bottom-4 left-4">
                <h3 class="text-xl font-bold text-white mb-1"><%= crew.name %></h3>
              </div>
            </div>

            <!-- Content -->
            <div class="p-6 flex-1 flex flex-col">
              <div class="flex items-center text-gray-600 text-sm mb-4">
                <svg class="h-4 w-4 mr-2 text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                </svg>
                Leader: <%= crew.leader.name %>
              </div>

              <% if crew.description.present? %>
                <p class="text-sm text-gray-500 mb-6 line-clamp-2 leading-relaxed"><%= crew.description %></p>
              <% end %>

              <div class="mt-auto pt-4 border-t border-gray-100">
                <span class="block w-full text-center py-3 bg-gray-100 text-gray-900 font-bold rounded-xl group-hover:bg-primary group-hover:text-white transition-all text-sm">
                  자세히 보기
                </span>
              </div>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>

  <% else %>
    <div class="py-24 text-center bg-gray-50 rounded-2xl border border-dashed border-gray-200 mt-12">
      <div class="w-16 h-16 bg-white rounded-2xl flex items-center justify-center mx-auto mb-6 text-gray-200 border border-gray-100">
        <svg class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"/></svg>
      </div>
      <h3 class="text-xl font-bold text-gray-900 mb-2">아직 생성된 크루가 없습니다</h3>
      <p class="text-gray-500 text-sm mb-6">첫 번째 크루를 만들어보세요!</p>
      <% if user_signed_in? %>
        <%= link_to "크루 만들기", new_crew_path, class: "inline-flex items-center px-6 py-3 bg-blue-600 text-white font-bold rounded-xl hover:bg-blue-700 transition-all text-sm" %>
      <% else %>
        <%= link_to "로그인하고 크루 만들기", new_user_session_path, class: "inline-flex items-center px-6 py-3 bg-blue-600 text-white font-bold rounded-xl hover:bg-blue-700 transition-all text-sm" %>
      <% end %>
    </div>
  <% end %>
</div>
