<div class="max-w-2xl mx-auto px-6 py-16 text-center">
  <div class="mb-12">
    <div class="w-24 h-24 bg-green-50 text-green-500 rounded-full flex items-center justify-center mx-auto mb-6 shadow-sm">
      <svg class="w-12 h-12" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
      </svg>
    </div>
    <h1 class="text-4xl font-black text-gray-900 tracking-tighter mb-4">참가 신청 완료! 🎉</h1>
    <p class="text-xl text-gray-500 font-medium leading-relaxed">
      <span class="text-blue-600 font-bold"><%= @registration.race.title %></span><br>
      참가 신청이 성공적으로 완료되었습니다.
    </p>
  </div>

  <!-- Ticket Preview -->
  <div class="bg-white rounded-[2rem] shadow-xl shadow-blue-50 overflow-hidden border border-gray-100 relative mb-12 text-left">
    <div class="absolute top-0 left-0 w-full h-2 bg-gradient-to-r from-blue-500 to-indigo-600"></div>
    
    <div class="p-10">
      <div class="flex flex-col md:flex-row md:justify-between md:items-start mb-8">
        <div>
           <span class="inline-block px-3 py-1 bg-green-50 text-green-600 text-[10px] font-black uppercase tracking-widest rounded-full mb-3">
             Paid
           </span>
           <h2 class="text-2xl font-black text-gray-900 mb-1"><%= @registration.race.title %></h2>
           <p class="text-lg font-bold text-gray-500"><%= @registration.race_edition.name %></p>
        </div>
        <div class="hidden md:block text-right">
           <p class="text-[10px] text-gray-400 font-black uppercase tracking-widest">Order Number</p>
           <p class="text-lg font-bold text-gray-900 tracking-widest"><%= @registration.merchant_uid.split('_').last %></p>
        </div>
      </div>
      
      <div class="grid md:grid-cols-2 gap-8 mb-8 border-t border-gray-100 pt-8">
         <div>
            <p class="text-[10px] text-gray-400 font-black uppercase tracking-widest mb-1">Date</p>
            <p class="text-lg font-bold text-gray-900"><%= @registration.race.start_at.strftime("%Y년 %m월 %d일") %></p>
         </div>
         <div>
            <p class="text-[10px] text-gray-400 font-black uppercase tracking-widest mb-1">Location</p>
            <p class="text-lg font-bold text-gray-900"><%= @registration.race.location %></p>
         </div>
      </div>
      
      <div class="flex justify-center bg-gray-50 rounded-2xl p-6 border border-gray-100 border-dashed">
         <div class="text-center">
            <%= render_qr_code(@registration.qr_token) if @registration.qr_token %>
            <p class="text-[10px] text-gray-400 font-black uppercase tracking-widest mt-2">Entry QR Code</p>
         </div>
      </div>
    </div>
  </div>

  <div class="space-y-4">
    <%= link_to "나의 티켓함 가기", registrations_path, class: "block w-full py-4 bg-gray-900 hover:bg-black text-white font-black rounded-2xl shadow-lg transition-all" %>
    <%= link_to "홈으로 돌아가기", root_path, class: "block w-full py-4 text-gray-500 font-bold hover:text-gray-900 transition-colors" %>
  </div>
</div>
