<div class="max-w-6xl mx-auto px-6 py-16">
  <% content_for :header_actions do %>
    <div class="text-right">
       <span class="text-sm text-gray-500 font-bold block">보유 티켓</span>
       <span class="text-3xl font-black text-blue-600"><%= @registrations.count %></span>
    </div>
  <% end %>

  <%= render 'shared/page_header', 
      title: "나의 티켓", 
      icon: "🎫", 
      description: "신청하신 대회 티켓을 확인하고 QR코드로 간편하게 체크인하세요." %>

  <% if @registrations.any? %>
    <div class="space-y-8">
      <% @registrations.each do |reg| %>
      <!-- Ticket Card -->
      <div class="bg-white rounded-[2.5rem] shadow-xl shadow-blue-50/50 overflow-hidden border border-gray-100 relative group transition-transform hover:-translate-y-1">
        <div class="absolute top-0 left-0 w-2 h-full bg-gradient-to-b from-blue-500 to-indigo-600"></div>
        
        <div class="flex flex-col md:flex-row">
          <!-- Left Info -->
          <div class="p-8 flex-1">
             <div class="flex items-center justify-between mb-6">
               <span class="px-3 py-1 bg-blue-50 text-blue-600 text-[10px] font-black uppercase tracking-widest rounded-full border border-blue-100">
                 <%= reg.status.upcase %>
               </span>
               <span class="text-xs font-bold text-gray-400">Order #<%= reg.merchant_uid.split('_').last %></span>
             </div>
             
             <h2 class="text-2xl font-black text-gray-900 mb-2 group-hover:text-blue-600 transition-colors">
               <%= link_to reg.race.title, race_path(reg.race) %>
             </h2>
             <p class="text-lg font-bold text-gray-600 mb-8">
               <%= reg.race_edition.name %>
             </p>
             
             <div class="grid grid-cols-2 gap-4 text-sm font-bold text-gray-500">
               <div class="flex items-center">
                 <svg class="w-5 h-5 mr-2 text-gray-300" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                   <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                 </svg>
                 <%= reg.race.start_at.strftime("%Y.%m.%d %H:%M") %>
               </div>
               <div class="flex items-center">
                 <svg class="w-5 h-5 mr-2 text-gray-300" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                   <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                   <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
                 </svg>
                 <%= reg.race.location %>
               </div>
             </div>
          </div>
          
          <!-- Right QR Ticket Stub -->
          <div class="relative bg-gray-50 p-8 flex flex-col items-center justify-center border-t md:border-t-0 md:border-l-2 border-dashed border-gray-200 min-w-[200px]">
            <!-- Semicircle Cutouts using mask or pseudo-elements (Simulated with background color circles) -->
            <div class="hidden md:block absolute -left-3 top-[-10px] w-6 h-6 bg-white rounded-full"></div> 
            <div class="hidden md:block absolute -left-3 bottom-[-10px] w-6 h-6 bg-white rounded-full"></div>
            
            <div class="bg-white p-3 rounded-xl shadow-sm mb-4 border border-gray-100">
              <!-- QR Code Helper -->
              <%= render_qr_code(reg.qr_token) if reg.qr_token.present? %>
            </div>
            <p class="text-[10px] text-gray-400 font-black uppercase tracking-widest text-center">Scan at Entry</p>
            
            <% if reg.status == 'paid' %>
              <div class="mt-4 px-4 py-1 bg-green-100 text-green-700 text-xs font-black rounded-full uppercase tracking-wide">confirmed</div>
            <% else %>
               <div class="mt-4 px-4 py-1 bg-gray-200 text-gray-500 text-xs font-black rounded-full uppercase tracking-wide">pending</div>
            <% end %>
          </div>
        </div>
      </div>
      <% end %>
    </div>
  <% else %>
    <!-- Empty State -->
    <div class="text-center py-24 bg-white rounded-[2.5rem] border border-gray-100 shadow-sm">
      <div class="w-24 h-24 bg-gray-50 rounded-full mx-auto mb-8 flex items-center justify-center">
        <svg class="w-10 h-10 text-gray-300" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 5v2m0 4v2m0 4v2M5 5a2 2 0 00-2 2v3a2 2 0 110 4v3a2 2 0 002 2h14a2 2 0 002-2v-3a2 2 0 110-4V7a2 2 0 00-2-2H5z" />
        </svg>
      </div>
      <h3 class="text-2xl font-black text-gray-900 mb-3 tracking-tight">아직 티켓이 없습니다</h3>
      <p class="text-gray-500 font-medium mb-10 max-w-sm mx-auto">
        지금 바로 러닝 대회에 참가 신청하고<br>나만의 레이스를 시작해보세요!
      </p>
      <%= link_to "참가 신청하러 가기", races_path, class: "inline-flex items-center px-8 py-4 bg-blue-600 hover:bg-blue-700 text-white font-black rounded-xl shadow-xl shadow-blue-200 transition-all active:scale-[0.98]" %>
    </div>
  <% end %>
</div>
