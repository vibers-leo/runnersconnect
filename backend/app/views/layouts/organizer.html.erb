<!DOCTYPE html>
<html>
  <head>
    <title>주최자 대시보드 - Runner's Connect</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="stylesheet" href="/tailwind.css" data-turbo-track="reload">
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 text-foreground min-h-screen font-sans antialiased">
    <!-- 주최자 전용 헤더 -->
    <header class="sticky top-0 z-50 w-full bg-white border-b border-gray-200">
      <nav class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
        <%= link_to organizer_root_path, class: "flex items-center space-x-3 group" do %>
          <div class="w-10 h-10 bg-indigo-600 rounded-xl flex items-center justify-center shadow-lg shadow-indigo-600/20 group-hover:scale-105 transition-transform duration-200">
            <svg class="h-6 w-6 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
            </svg>
          </div>
          <div>
            <span class="text-lg font-black text-gray-900 tracking-tighter block">주최자 대시보드</span>
            <span class="text-xs text-gray-500 font-bold">Runner's Connect</span>
          </div>
        <% end %>

        <div class="flex items-center gap-6 text-sm font-bold text-gray-600">
          <%= link_to "대회 목록", organizer_races_path, class: "hover:text-indigo-600 transition-colors #{current_page?(organizer_races_path) ? 'text-indigo-600' : ''}" %>

          <div class="flex items-center gap-4 pl-6 border-l border-gray-200 ml-4">
            <% if current_user.avatar.attached? %>
              <%= image_tag current_user.avatar.variant(:thumb), class: "w-8 h-8 rounded-full object-cover ring-2 ring-gray-100" %>
            <% else %>
              <div class="w-8 h-8 rounded-full bg-gradient-to-br from-indigo-500 to-purple-600 flex items-center justify-center text-white text-xs font-black ring-2 ring-gray-100">
                <%= current_user.display_name[0].upcase %>
              </div>
            <% end %>
            <div>
              <p class="text-sm font-bold text-gray-900"><%= current_user.display_name %></p>
              <% if current_organizer_profile %>
                <p class="text-xs text-gray-500"><%= current_organizer_profile.business_name %></p>
              <% end %>
            </div>
            <%= link_to "로그아웃", destroy_user_session_path, method: :delete, class: "ml-4 text-xs font-bold text-gray-500 hover:text-red-600 transition-colors" %>
          </div>
        </div>
      </nav>
    </header>

    <!-- 사이드바 + 메인 컨텐츠 레이아웃 -->
    <div class="flex max-w-7xl mx-auto">
      <!-- 사이드바 네비게이션 -->
      <aside class="hidden md:block w-64 bg-white border-r border-gray-200 min-h-[calc(100vh-73px)]">
        <nav class="p-6 space-y-2">
          <%= link_to organizer_root_path, class: "flex items-center space-x-3 px-4 py-3 rounded-lg font-bold text-sm transition-colors #{current_page?(organizer_root_path) ? 'bg-indigo-50 text-indigo-600' : 'text-gray-600 hover:bg-gray-50'}" do %>
            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
            </svg>
            <span>대시보드</span>
          <% end %>

          <%= link_to organizer_races_path, class: "flex items-center space-x-3 px-4 py-3 rounded-lg font-bold text-sm transition-colors #{current_page?(organizer_races_path) ? 'bg-indigo-50 text-indigo-600' : 'text-gray-600 hover:bg-gray-50'}" do %>
            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
            </svg>
            <span>내 대회</span>
          <% end %>

          <div class="pt-4 mt-4 border-t border-gray-200">
            <p class="px-4 text-xs font-black text-gray-400 uppercase tracking-wider mb-2">관리 도구</p>

            <a href="#" class="flex items-center space-x-3 px-4 py-3 rounded-lg font-bold text-sm text-gray-400 cursor-not-allowed">
              <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
              </svg>
              <span>참가자 관리</span>
              <span class="ml-auto text-xs bg-gray-100 text-gray-500 px-2 py-1 rounded">준비중</span>
            </a>

            <a href="#" class="flex items-center space-x-3 px-4 py-3 rounded-lg font-bold text-sm text-gray-400 cursor-not-allowed">
              <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
              <span>결제 현황</span>
              <span class="ml-auto text-xs bg-gray-100 text-gray-500 px-2 py-1 rounded">준비중</span>
            </a>

            <a href="#" class="flex items-center space-x-3 px-4 py-3 rounded-lg font-bold text-sm text-gray-400 cursor-not-allowed">
              <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v13m0-13V6a2 2 0 112 2h-2zm0 0V5.5A2.5 2.5 0 109.5 8H12zm-7 4h14M5 12a2 2 0 110-4h14a2 2 0 110 4M5 12v7a2 2 0 002 2h10a2 2 0 002-2v-7" />
              </svg>
              <span>기념품 현황</span>
              <span class="ml-auto text-xs bg-gray-100 text-gray-500 px-2 py-1 rounded">준비중</span>
            </a>
          </div>
        </nav>
      </aside>

      <!-- 메인 컨텐츠 영역 -->
      <main class="flex-1 p-6 md:p-8">
        <% if notice %>
          <div class="mb-6 animate-fade-in-down">
            <div class="bg-blue-50 border border-blue-200 text-blue-700 px-6 py-4 rounded-xl text-sm font-bold flex items-center">
              <svg class="w-5 h-5 mr-3" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
              <%= notice %>
            </div>
          </div>
        <% end %>

        <% if alert %>
          <div class="mb-6 animate-fade-in-down">
            <div class="bg-red-50 border border-red-200 text-red-700 px-6 py-4 rounded-xl text-sm font-bold flex items-center">
              <svg class="w-5 h-5 mr-3" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" /></svg>
              <%= alert %>
            </div>
          </div>
        <% end %>

        <%= yield %>
      </main>
    </div>
  </body>
</html>
