<!DOCTYPE html>
<html>
  <head>
    <title>Runner's Connect</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <!-- Portone Payment SDK -->
    <script src="https://cdn.iamport.kr/v1/iamport.js"></script>

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "shadcn", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-background text-foreground min-h-screen relative font-sans antialiased">
    <header class="sticky top-0 z-50 w-full bg-white/80 backdrop-blur-md border-b border-border transition-all duration-300">
      <nav class="max-w-6xl mx-auto px-6 py-4 flex items-center justify-between">
        <%= link_to root_path, class: "flex items-center space-x-3 group" do %>
          <div class="w-10 h-10 bg-primary rounded-xl flex items-center justify-center shadow-lg shadow-primary/20 group-hover:scale-105 transition-transform duration-200">
            <svg class="h-6 w-6 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M13 10V3L4 14h7v7l9-11h-7z" />
            </svg>
          </div>
          <span class="text-xl font-black text-gray-900 tracking-tighter">Runner's Connect</span>
        <% end %>

        <div class="hidden md:flex items-center gap-6 text-sm font-bold text-muted-foreground uppercase tracking-widest">
          <%= link_to "대회 목록", races_path, class: "hover:text-primary transition-all duration-200 #{current_page?(races_path) ? 'text-primary' : ''}" %>
          <%= link_to "랭킹", rankings_path, class: "hover:text-primary transition-all duration-200 #{current_page?(rankings_path) ? 'text-primary' : ''}" %>

          <% if user_signed_in? %>
            <%= link_to "나의 티켓", registrations_path, class: "hover:text-primary transition-all duration-200 #{current_page?(registrations_path) ? 'text-primary' : ''}" %>
            <%= link_to "나의 기록", records_path, class: "hover:text-primary transition-all duration-200 #{current_page?(records_path) ? 'text-primary' : ''}" %>
            <%= link_to "크루", crews_path, class: "hover:text-primary transition-all duration-200 #{current_page?(crews_path) ? 'text-primary' : ''}" %>
            
            <div class="flex items-center gap-4 pl-6 border-l border-gray-100 ml-4">
              <%= link_to profile_path, class: "flex items-center space-x-3 hover:opacity-80 transition-opacity" do %>
                <% if current_user.avatar.attached? %>
                  <%= image_tag current_user.avatar.variant(:thumb), class: "w-8 h-8 rounded-full object-cover ring-2 ring-gray-100" %>
                <% else %>
                  <div class="w-8 h-8 rounded-full bg-gradient-to-br from-blue-500 to-indigo-600 flex items-center justify-center text-white text-xs font-black ring-2 ring-gray-100">
                    <%= current_user.display_name[0].upcase %>
                  </div>
                <% end %>
                <span class="text-gray-900 font-bold"><%= current_user.display_name %></span>
              <% end %>
              <%= render_button "로그아웃", as: :button, method: :delete, href: destroy_user_session_path, variant: :secondary, class: "text-[10px] font-black h-8 px-4 rounded-full bg-gray-50 text-gray-500 hover:bg-red-50 hover:text-red-500 border-none transition-all" %>
            </div>
          <% else %>
            <%= render_button "로그인", as: :link, href: new_user_session_path, class: "bg-gray-900 text-white font-bold px-6 h-10 rounded-xl hover:bg-gray-800 shadow-md hover:shadow-lg transition-all active:scale-95 text-xs border-none" %>
          <% end %>
        </div>
      </nav>
    </header>

    <main class="pb-24 md:pb-0 min-h-[calc(100vh-160px)]">
      <% if notice %>
        <div class="max-w-6xl mx-auto px-6 mt-6 animate-fade-in-down">
          <div class="bg-blue-50 border border-blue-100/50 text-blue-600 px-6 py-4 rounded-xl text-sm font-bold flex items-center justify-center">
            <svg class="w-5 h-5 mr-3" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
            <%= notice %>
          </div>
        </div>
      <% end %>
      
      <% if alert %>
        <div class="max-w-6xl mx-auto px-6 mt-6 animate-fade-in-down">
          <div class="bg-red-50 border border-red-100/50 text-red-600 px-6 py-4 rounded-xl text-sm font-bold flex items-center justify-center">
            <svg class="w-5 h-5 mr-3" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" /></svg>
            <%= alert %>
          </div>
        </div>
      <% end %>

      <%= yield %>
    </main>
    
    <!-- Mobile Bottom Navigation -->
    <nav class="md:hidden fixed bottom-0 left-0 w-full bg-white/95 backdrop-blur-xl border-t border-gray-100 px-6 py-3 flex justify-between items-center z-50 pb-safe shadow-[0_-4px_10px_rgba(0,0,0,0.05)]">
       <% nav_items = [
         { path: root_path, icon: "M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6", label: "홈" },
         { path: races_path, icon: "M13 10V3L4 14h7v7l9-11h-7z", label: "대회" },
         { path: registrations_path, icon: "M15 5v2m0 4v2m0 4v2M5 5a2 2 0 00-2 2v3a2 2 0 110 4v3a2 2 0 002 2h14a2 2 0 002-2v-3a2 2 0 110-4V7a2 2 0 00-2-2H5z", label: "티켓", auth: true },
         { path: crews_path, icon: "M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z", label: "크루", auth: true },
         { path: profile_path, icon: "M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z", label: "MY", auth: true }
       ] %>

       <% nav_items.each do |item| %>
         <% if !item[:auth] || user_signed_in? %>
           <%= link_to item[:path], class: "flex flex-col items-center transition-all duration-200 #{current_page?(item[:path]) ? 'text-primary' : 'text-muted-foreground hover:text-foreground'}" do %>
             <svg class="w-6 h-6 mb-1" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
               <path stroke-linecap="round" stroke-linejoin="round" d="<%= item[:icon] %>" />
             </svg>
             <span class="text-[10px] font-bold tracking-tight"><%= item[:label] %></span>
           <% end %>
         <% end %>
       <% end %>

       <% if !user_signed_in? %>
         <%= link_to new_user_session_path, class: "flex flex-col items-center text-gray-400 hover:text-gray-900 transition-all" do %>
            <svg class="w-6 h-6 mb-1" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1" /></svg>
            <span class="text-[10px] font-bold tracking-tight">로그인</span>
         <% end %>
       <% end %>
    </nav>

    <footer class="hidden md:block bg-white border-t border-gray-200 py-16">
      <div class="max-w-6xl mx-auto px-6">
        <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-12 mb-16">
          <div class="col-span-2">
            <div class="flex items-center space-x-2 mb-6">
              <div class="w-8 h-8 bg-gray-900 rounded-lg flex items-center justify-center shadow-lg">
                <svg class="h-4 w-4 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M13 10V3L4 14h7v7l9-11h-7z" />
                </svg>
              </div>
              <span class="text-xl font-black text-gray-900 tracking-tighter">Runner's Connect</span>
            </div>
            <p class="text-gray-500 font-medium max-w-sm leading-relaxed text-sm">
              우리는 모든 러너들의 열정을 연결하고, 당신의 한계를 뛰어넘는 여정을 함께합니다.
            </p>
          </div>
          
          <div>
            <h4 class="text-xs font-black text-gray-900 uppercase tracking-widest mb-6">Explore</h4>
            <div class="flex flex-col space-y-3 text-sm font-bold text-gray-400">
              <%= link_to "대회 목록", races_path, class: "hover:text-blue-600 transition-colors" %>
              <%= link_to "랭킹 현황", rankings_path, class: "hover:text-blue-600 transition-colors" %>
              <%= link_to "크루 찾기", crews_path, class: "hover:text-blue-600 transition-colors" %>
            </div>
          </div>

          <div>
            <h4 class="text-xs font-black text-gray-900 uppercase tracking-widest mb-6">Support</h4>
            <div class="flex flex-col space-y-3 text-sm font-bold text-gray-400">
              <a href="#" class="hover:text-blue-600 transition-colors">이용약관</a>
              <a href="#" class="hover:text-blue-600 transition-colors">개인정보처리방침</a>
              <a href="#" class="hover:text-blue-600 transition-colors">고객센터</a>
            </div>
          </div>
        </div>

        <div class="pt-8 border-t border-gray-100 flex flex-col md:flex-row justify-between items-center gap-4 text-center md:text-left">
          <div class="text-[11px] text-gray-400 font-bold leading-relaxed space-y-1">
            <p>(주)러너스커넥트 | 대표: 홍길동 | 사업자등록번호: 123-45-67890</p>
            <p>서울시 강남구 테헤란로 123번지 4층 | 고객센터: 1588-0000</p>
          </div>
          <p class="text-[11px] text-gray-400 font-bold uppercase tracking-widest">
            © 2026 Runner's Connect. All rights reserved.
          </p>
        </div>
      </div>
    </footer>
  </body>
</html>
