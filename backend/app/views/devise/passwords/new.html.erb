<div class="min-h-[calc(100-73px)] flex flex-col items-center justify-center p-6 sm:p-12">
  <div class="max-w-md w-full">
    <div class="text-center mb-10">
      <h2 class="text-4xl font-black text-gray-900 tracking-tighter mb-2">비밀번호 찾기 🔒</h2>
      <p class="text-gray-500 font-medium">가입하신 이메일 주소를 입력해주세요.</p>
    </div>

    <div class="bg-white rounded-[2.5rem] shadow-xl shadow-blue-50 border border-gray-100 p-8 sm:p-10">
      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "space-y-6" }) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div>
          <%= f.label :email, "이메일 주소", class: "block text-sm font-bold text-gray-700 mb-2" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "runners@example.com", class: "block w-full px-5 py-4 rounded-2xl border-gray-200 focus:ring-blue-500 focus:border-blue-500 shadow-sm transition-all" %>
        </div>

        <div class="pt-4">
          <%= f.submit "비밀번호 재설정 링크 보내기", class: "w-full py-5 bg-blue-600 hover:bg-blue-700 text-white font-black rounded-2xl shadow-xl shadow-blue-100 transition-all active:scale-[0.98] text-lg" %>
        </div>
      <% end %>

      <div class="mt-8 text-center pt-8 border-t border-gray-50">
        <p class="text-sm text-gray-400 font-bold mb-4">비밀번호가 기억나셨나요?</p>
        <%- if controller_name != 'sessions' %>
          <%= link_to "로그인하러 가기", new_session_path(resource_name), class: "inline-block py-3 px-8 bg-gray-50 hover:bg-gray-100 text-gray-900 font-black rounded-xl transition-all" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
