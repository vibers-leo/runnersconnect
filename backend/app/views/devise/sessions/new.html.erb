<div class="min-h-[calc(100-73px)] flex flex-col items-center justify-center p-6 sm:p-12">
  <div class="max-w-md w-full">
    <div class="text-center mb-10">
      <h2 class="text-4xl font-black text-gray-900 tracking-tighter mb-2">반가워요! 👋</h2>
      <p class="text-gray-500 font-medium">러너스 커넥트에 오신 것을 환영합니다.</p>
    </div>

    <div class="bg-white rounded-[2.5rem] shadow-xl shadow-blue-50 border border-gray-100 p-8 sm:p-10">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "space-y-6" }) do |f| %>
        <div>
          <%= f.label :email, "이메일 주소", class: "block text-sm font-bold text-gray-700 mb-2" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "runners@example.com", class: "block w-full px-5 py-4 rounded-2xl border-gray-200 focus:ring-blue-500 focus:border-blue-500 shadow-sm transition-all" %>
        </div>

        <div>
          <div class="flex justify-between items-center mb-2">
            <%= f.label :password, "비밀번호", class: "block text-sm font-bold text-gray-700" %>
            <%- if devise_mapping.recoverable? && controller_name != 'passwords' && action_name != 'edit' %>
              <%= link_to "비밀번호 찾기", new_password_path(resource_name), class: "text-xs font-bold text-blue-600 hover:text-blue-700" %>
            <% end %>
          </div>
          <%= f.password_field :password, autocomplete: "current-password", placeholder: "••••••••", class: "block w-full px-5 py-4 rounded-2xl border-gray-200 focus:ring-blue-500 focus:border-blue-500 shadow-sm transition-all" %>
        </div>

        <% if devise_mapping.rememberable? %>
          <div class="flex items-center">
            <%= f.check_box :remember_me, class: "h-5 w-5 text-blue-600 focus:ring-blue-500 border-gray-300 rounded-lg mr-2" %>
            <%= f.label :remember_me, "로그인 유지", class: "text-sm font-bold text-gray-500" %>
          </div>
        <% end %>

        <div class="pt-4">
          <%= f.submit "로그인", class: "w-full py-5 bg-blue-600 hover:bg-blue-700 text-white font-black rounded-2xl shadow-xl shadow-blue-100 transition-all active:scale-[0.98] text-lg" %>
        </div>
      <% end %>

      <div class="mt-8 text-center pt-8 border-t border-gray-50">
        <p class="text-sm text-gray-400 font-bold mb-4">아직 계정이 없으신가요?</p>
        <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
          <%= link_to "30초 만에 회원가입", new_user_registration_path, class: "inline-block py-3 px-8 bg-gray-50 hover:bg-gray-100 text-gray-900 font-black rounded-xl transition-all" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
