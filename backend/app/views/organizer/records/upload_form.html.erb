<div class="max-w-4xl mx-auto space-y-6">
  <!-- 페이지 헤더 -->
  <div class="flex items-center justify-between">
    <div>
      <h1 class="text-3xl font-black text-gray-900"><%= @race.title %> - 기록 업로드</h1>
      <p class="text-gray-500 font-medium mt-2">CSV 파일로 참가자 기록을 일괄 업로드할 수 있습니다</p>
    </div>
    <%= link_to "← 기록 목록", organizer_race_records_path(@race), class: "text-sm font-bold text-gray-600 hover:text-gray-900" %>
  </div>

  <!-- 업로드 방법 안내 -->
  <div class="bg-blue-50 rounded-2xl border border-blue-200 p-6">
    <div class="flex items-start gap-4">
      <div class="w-10 h-10 bg-blue-500 rounded-lg flex items-center justify-center flex-shrink-0">
        <svg class="w-6 h-6 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
      </div>
      <div class="flex-1">
        <h2 class="text-lg font-black text-blue-900 mb-3">업로드 방법</h2>
        <ol class="space-y-2 text-sm text-blue-800">
          <li class="flex items-start gap-2">
            <span class="inline-flex items-center justify-center w-6 h-6 bg-blue-500 text-white font-bold rounded-full flex-shrink-0 text-xs">1</span>
            <span><strong>CSV 파일을 준비</strong>해주세요. (등번호, 완주 시간 필수)</span>
          </li>
          <li class="flex items-start gap-2">
            <span class="inline-flex items-center justify-center w-6 h-6 bg-blue-500 text-white font-bold rounded-full flex-shrink-0 text-xs">2</span>
            <span><strong>완주 시간 형식</strong>: <code class="bg-blue-100 px-2 py-1 rounded font-mono">1:23:45</code> (시:분:초) 또는 <code class="bg-blue-100 px-2 py-1 rounded font-mono">23:45</code> (분:초)</span>
          </li>
          <li class="flex items-start gap-2">
            <span class="inline-flex items-center justify-center w-6 h-6 bg-blue-500 text-white font-bold rounded-full flex-shrink-0 text-xs">3</span>
            <span>샘플 파일을 참고하시면 더 쉽게 작성할 수 있습니다</span>
          </li>
          <li class="flex items-start gap-2">
            <span class="inline-flex items-center justify-center w-6 h-6 bg-blue-500 text-white font-bold rounded-full flex-shrink-0 text-xs">4</span>
            <span><strong>인코딩 자동 감지</strong>: UTF-8, EUC-KR, CP949 형식을 모두 지원합니다 (엑셀에서 저장한 파일도 OK)</span>
          </li>
        </ol>
        <div class="mt-4">
          <%= link_to download_sample_organizer_race_records_path(@race), class: "inline-flex items-center px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white font-bold rounded-lg transition-colors text-sm" do %>
            <svg class="w-4 h-4 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
            </svg>
            샘플 파일 다운로드
          <% end %>
        </div>
      </div>
    </div>
  </div>

  <!-- CSV 파일 예시 -->
  <div class="bg-white rounded-2xl border border-gray-200 shadow-sm overflow-hidden">
    <div class="px-6 py-4 border-b border-gray-200 bg-gray-50">
      <h3 class="text-lg font-black text-gray-900">CSV 파일 예시</h3>
    </div>
    <div class="p-6">
      <div class="bg-gray-900 rounded-lg p-4 overflow-x-auto">
        <pre class="text-green-400 font-mono text-sm"><code>등번호,완주 시간
1,1:23:45
2,1:25:30
3,1:28:15
4,45:30
5,52:15</code></pre>
      </div>
      <p class="text-sm text-gray-500 mt-3">
        <strong>주의:</strong> 첫 번째 행은 반드시 "등번호,완주 시간" 헤더여야 합니다.
      </p>
    </div>
  </div>

  <!-- 업로드 폼 -->
  <div class="bg-white rounded-2xl border border-gray-200 shadow-sm overflow-hidden">
    <div class="px-6 py-4 border-b border-gray-200 bg-gray-50">
      <h3 class="text-lg font-black text-gray-900">파일 업로드</h3>
    </div>
    <div class="p-6">
      <%= form_with url: bulk_upload_organizer_race_records_path(@race), method: :post, multipart: true, class: "space-y-6" do |f| %>
        <div>
          <%= f.label :records_file, 'CSV 파일 선택', class: 'block text-sm font-bold text-gray-700 mb-2' %>
          <div class="mt-1 flex justify-center px-6 pt-5 pb-6 border-2 border-gray-300 border-dashed rounded-lg hover:border-indigo-400 transition-colors">
            <div class="space-y-1 text-center">
              <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12" />
              </svg>
              <div class="flex text-sm text-gray-600">
                <label for="records_file" class="relative cursor-pointer bg-white rounded-md font-medium text-indigo-600 hover:text-indigo-500 focus-within:outline-none">
                  <span>파일을 선택하거나</span>
                  <%= f.file_field :records_file, accept: '.csv', required: true, class: 'sr-only', id: 'records_file' %>
                </label>
                <p class="pl-1">드래그 앤 드롭</p>
              </div>
              <p class="text-xs text-gray-500">CSV 파일만 업로드 가능</p>
            </div>
          </div>
        </div>

        <div class="flex items-center justify-end gap-3 pt-4 border-t border-gray-200">
          <%= link_to '취소', organizer_race_records_path(@race), class: 'px-6 py-3 border-2 border-gray-300 text-gray-700 font-bold rounded-lg hover:bg-gray-50 transition-colors' %>
          <%= f.submit '업로드', class: 'px-6 py-3 bg-indigo-600 hover:bg-indigo-700 text-white font-bold rounded-lg transition-colors cursor-pointer' %>
        </div>
      <% end %>
    </div>
  </div>

  <!-- 주의사항 -->
  <div class="bg-yellow-50 rounded-2xl border border-yellow-200 p-6">
    <div class="flex items-start gap-4">
      <div class="w-10 h-10 bg-yellow-500 rounded-lg flex items-center justify-center flex-shrink-0">
        <svg class="w-6 h-6 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
        </svg>
      </div>
      <div class="flex-1">
        <h3 class="text-lg font-black text-yellow-900 mb-2">주의사항</h3>
        <ul class="space-y-1 text-sm text-yellow-800">
          <li class="flex items-start gap-2">
            <span class="text-yellow-600 mt-1">•</span>
            <span>등번호가 일치하지 않으면 해당 기록은 업로드되지 않습니다</span>
          </li>
          <li class="flex items-start gap-2">
            <span class="text-yellow-600 mt-1">•</span>
            <span>동일한 참가자의 기록이 이미 있는 경우 새로운 기록으로 덮어씌워집니다</span>
          </li>
          <li class="flex items-start gap-2">
            <span class="text-yellow-600 mt-1">•</span>
            <span>시간 형식이 올바르지 않으면 해당 기록은 건너뜁니다</span>
          </li>
        </ul>
      </div>
    </div>
  </div>
</div>
